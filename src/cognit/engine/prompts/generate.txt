Generate a comprehension quiz for the PR author. You are inspecting **PR #{pr_number}** on branch `{branch}`.

<pr_title>{pr_title}</pr_title>

<pr_body>
{pr_body}
</pr_body>

Inspect the change yourself, then submit the quiz:

1. Call `pr_overview` to list the changed files with their +/- line counts (vendored/minified/lock/binary files are already excluded).
2. For each file worth quizzing, call `file_diff(path)` to pull just that file's changes — don't fetch files you won't use. For surrounding *unchanged* context (a helper a changed function calls, a referenced type), use `Read`/`Grep`/`Glob` on the working tree (already checked out on this branch); read targeted line ranges, **never whole large/generated/vendored files**.
3. Decide the question count and type-mix yourself.
4. Submit the complete quiz — mermaid questions fully rendered — via the `submit_quiz` tool. A validator checks every diagram; if it rejects any, fix those and submit again.
